# Playbook to setup DNS server in sg-lab.pl domain
#
# Created by Daniel Buk
#
# Requires:
# bertvv.bind
---
- name: Include vars of dns_vars.yaml.
  include_vars:
    file: dns_vars.yaml

- hosts: dns
  become: true
  roles:
    - bertvv.bind
