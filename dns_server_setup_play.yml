# Playbook to setup DNS server in sg-lab.pl domain
#
# Created by Daniel Buk
#
# Requires:
# bertvv.bind
---
- hosts: dns
  become: true
  include_vars:
    file: dns_vars.yaml
  roles:
    - bertvv.bind
