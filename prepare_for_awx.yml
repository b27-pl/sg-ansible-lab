# Playbook used to prepare hosts to 
# be managed be Ansivble with AWX
#
---
- hosts: "{{ survey_hosts }}"
  roles:
    - prepare-os
  handlers:
    - name: restart ssh
      service:
        name: sshd
        state: restarted
